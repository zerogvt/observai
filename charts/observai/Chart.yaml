apiVersion: v2
name: observai
description: AI observability portfolio stack — gateway + inference (Ollama/llama3.2), OpenTelemetry Collector to Dynatrace, and Redis for distributed rate limiting.
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Version of the app (gateway/inference images) this chart deploys by default.
appVersion: "0.1.0"
keywords:
  - observability
  - opentelemetry
  - dynatrace
  - llm
  - ollama
maintainers:
  - name: observai
